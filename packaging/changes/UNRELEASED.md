# Unreleased

This is the development ledger between versioned builds. Keep
`0.4.132-beta` as the declared product version until the repository owner
chooses the next version. At release time, move verified bullets into the new
versioned `.txt` change file.

## Verified

- Optimized Match Setup, Agent Performance, snapshot projection, cross-session
  search, control-plane input, Rolling 80% history selection, provider prompt
  preparation and tracing, secure snapshot persistence, and live-agent card
  reconciliation. The batch preserves provider bytes and persisted semantics,
  adds bounded caches and privacy-safe metadata paths, and includes focused
  performance, concurrency, cancellation, accessibility, and equivalence gates.
- Optimized diagnostics, Agent and Collaborate conversations, Models layout,
  Experiment serialization and loading, event logging, AI World rendering,
  telemetry, Help search, Internet DNS validation, QA orchestration, structural
  test reads, and Status Center scheduling. Release receipts, installer resume,
  checksum generation, accessibility behavior, cancellation, durability, and
  deterministic-output gates were hardened alongside measured performance tests.
