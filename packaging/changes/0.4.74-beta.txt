AI Arena 0.4.74-beta

- Add rebuild-diffing to AI World: when a snapshot is render-identical to the current scene (a common case on refresh ticks and view switches), the full scene teardown and rebuild is skipped and the running animation simply continues. Any content or theme change still triggers a rebuild.
- Add regression coverage proving identical snapshots skip the rebuild while changed snapshots rebuild exactly once.
