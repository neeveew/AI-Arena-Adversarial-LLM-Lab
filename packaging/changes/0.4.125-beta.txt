- Adds Agent's native .NET Solution Doctor: bounded `.sln`, `.slnx`, and `.csproj` discovery; target frameworks, references, WPF/output/test classification; restore-state diagnostics; and project-correct Build, Test, Run, and separately approved Restore plans.
- Turns approved `dotnet` output into structured C# and MSBuild diagnostics, safe relative locations, warning/error totals, test totals, failing-test evidence, and narrowed Stage Repair/Retry guidance while preserving the original stdout and stderr.
- Keeps command preview and workspace boundaries authoritative: restore stays a visible network/install risk, PowerShell suggestions quote hostile paths literally, canceled or stale results cannot contaminate a switched workspace, partial discovery is labelled instead of guessed, and no Solution Doctor logic is placed in MainWindow.
